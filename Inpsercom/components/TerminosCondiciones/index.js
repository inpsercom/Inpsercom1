
app.terminoscon = kendo.observable({
    onShow: function () {
      //  $("#NumeroChasisTC").text(datos_Cliente.chasis);
       

// RRP: alias - terminos condio
        //$("#NumeroChasisTC").text(datos_Cliente.nombre_alias);  
 },
    afterShow: function () { },
    inicializa: function () { },
});
app.localization.registerView('terminoscon');